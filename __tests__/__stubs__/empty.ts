// Stub for the Next.js `server-only` guard module under vitest.
// At runtime in Next.js this throws if accidentally imported into
// a client bundle. Under unit tests we invoke the route function
// directly, so the guard is moot · this file is the alias target.
export {}
